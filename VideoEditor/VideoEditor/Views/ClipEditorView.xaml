<UserControl x:Class="VideoEditor.Views.ClipEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideoEditor.Views"
             xmlns:models="clr-namespace:VideoEditor.Models"
             xmlns:common="clr-namespace:VideoEditor.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">

    <UserControl.Resources>
        <common:ClipTypeToStringConverter x:Key="ClipTypeToStringConverter" />
    </UserControl.Resources>

    <ContentControl Content="{Binding}">
        <ContentControl.Resources>
            <!-- TextClip 편집기 -->
            <DataTemplate DataType="{x:Type models:TextClip}">
                <StackPanel Margin="10">
                    <TextBlock Text="자막 편집" FontWeight="Bold" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="내용:" Foreground="White" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                             AcceptsReturn="True" TextWrapping="Wrap" Height="100" VerticalScrollBarVisibility="Auto"/>
                    <TextBlock Text="이름:" Foreground="White" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
                </StackPanel>
            </DataTemplate>

            <!-- 다른 클립 타입들을 위한 정보 뷰 (Video, Image, Audio) -->
            <DataTemplate DataType="{x:Type models:TimelineClipBase}">
                <StackPanel Margin="10">
                    <TextBlock Text="클립 정보" FontWeight="Bold" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="이름:" Foreground="White" FontWeight="Bold" />
                    <TextBlock Text="{Binding Name}" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,5" />
                    <TextBlock Text="타입:" Foreground="White" FontWeight="Bold" />
                    <TextBlock Text="{Binding Converter={StaticResource ClipTypeToStringConverter}}" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,5" />
                    <TextBlock Text="길이:" Foreground="White" FontWeight="Bold" />
                    <TextBlock Text="{Binding Duration, StringFormat={}{0:F2}초}" Foreground="White" Margin="0,0,0,5" />
                </StackPanel>
            </DataTemplate>
        </ContentControl.Resources>
    </ContentControl>
</UserControl>
