<UserControl x:Class="VideoEditor.Views.TransformEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideoEditor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="280">
    <StackPanel>
        <Separator Margin="0,15,0,15"/>
        <TextBlock Text="변환" FontWeight="Bold" Foreground="White" FontSize="14" Margin="0,0,0,10"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 위치 X -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="위치 X:" Foreground="White" VerticalAlignment="Center"/>
            <Slider Grid.Row="0" Grid.Column="1" Minimum="-1000" Maximum="1000" Value="{Binding PositionX, Mode=TwoWay}"/>
            <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding PositionX, Mode=TwoWay, StringFormat={}{0:F0}}" Margin="5,0,0,0"/>

            <!-- 위치 Y -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="위치 Y:" Foreground="White" VerticalAlignment="Center"/>
            <Slider Grid.Row="1" Grid.Column="1" Minimum="-1000" Maximum="1000" Value="{Binding PositionY, Mode=TwoWay}"/>
            <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding PositionY, Mode=TwoWay, StringFormat={}{0:F0}}" Margin="5,0,0,0"/>

            <!-- 크기 -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="크기:" Foreground="White" VerticalAlignment="Center"/>
            <Slider Grid.Row="2" Grid.Column="1" Minimum="0.1" Maximum="5.0" Value="{Binding Scale, Mode=TwoWay}"/>
            <TextBox Grid.Row="2" Grid.Column="2" Text="{Binding Scale, Mode=TwoWay, StringFormat={}{0:F2}}" Margin="5,0,0,0"/>
        </Grid>
    </StackPanel>
</UserControl>
