<UserControl x:Class="VideoEditor.Views.VideoBackgroundLayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:VideoEditor.Models"
             xmlns:common="clr-namespace:VideoEditor.Common"
             xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <common:TypeToBoolConverter x:Key="TypeToBoolConverter" />

        <!-- DataTemplate for VideoClip - displayed as background -->
        <DataTemplate DataType="{x:Type models:VideoClip}">
            <vlc:VideoView MediaPlayer="{Binding MediaPlayer}"/>
        </DataTemplate>

    </UserControl.Resources>
    
    <!-- Only display VideoClips here, they act as the background video layer -->
    <ItemsControl ItemsSource="{Binding ActiveVisualClips}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Grid />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <!-- Only show VideoClip types -->
                <Style.Triggers>
                    <DataTrigger Binding="{Binding}" Value="{x:Null}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Converter={StaticResource TypeToBoolConverter}, ConverterParameter={x:Type models:ImageClip}}" Value="True">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Converter={StaticResource TypeToBoolConverter}, ConverterParameter={x:Type models:TextClip}}" Value="True">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ItemsControl.ItemContainerStyle>
    </ItemsControl>
</UserControl>
